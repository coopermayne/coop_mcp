# Web frontend. Also needs the MCP server's deps (fastmcp, jellyfish) because it
# imports server.py for its read functions — install ../requirements.txt too.
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
authlib>=1.3
itsdangerous>=2.1
httpx>=0.27
# In-app AI chat (/chat): drives server.py's journal/drinking tools via the
# Anthropic API. Optional at runtime — the chat surface only turns on when
# ANTHROPIC_API_KEY is set; the rest of the read-only app runs without it.
anthropic>=0.40
